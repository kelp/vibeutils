Usage: tail [OPTION]... [FILE]...
Print the last 10 lines of each FILE to standard output.
With more than one FILE, precede each with a header giving the file name.

With no FILE, or when FILE is -, read standard input.

Mandatory arguments to long options are mandatory for short options too.
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-c\[1m-c, --bytes=[+]NUM[0m]8;;\
         output the last NUM bytes;
         or use -c +NUM to output starting with byte NUM of each file
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail--debug\[1m--debug[0m]8;;\
         indicate which --follow implementation is used
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-f\[1m-f, --follow[={name|descriptor}][0m]8;;\
         output appended data as the file grows;
         an absent option argument means 'descriptor'
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-F\[1m-F[0m]8;;\
         same as --follow=name --retry
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-n\[1m-n, --lines=[+]NUM[0m]8;;\
         output the last NUM lines, instead of the last 10;
         or use -n +NUM to skip NUM-1 lines at the start
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail--max-unchanged-stats\[1m--max-unchanged-stats=N[0m]8;;\
         with --follow=name, reopen a FILE which has not
         changed size after N (default 5) iterations
         to see if it has been unlinked or renamed
         (this is the usual case of rotated log files);
         with inotify, this option is rarely useful
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail--pid\[1m--pid=PID[0m]8;;\
         with -f, exit after PID no longer exists;
         can be repeated to watch multiple processes
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-q\[1m-q, --quiet, --silent[0m]8;;\
         never output headers giving file names
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail--retry\[1m--retry[0m]8;;\
         keep trying to open a file if it is inaccessible
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-s\[1m-s, --sleep-interval=N[0m]8;;\
         with -f, sleep for approximately N seconds
         (default 1.0) between iterations;
         with inotify and --pid=P,
         check process P at least once every N seconds
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-v\[1m-v, --verbose[0m]8;;\
         always output headers giving file names
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#tail-z\[1m-z, --zero-terminated[0m]8;;\
         line delimiter is NUL, not newline
      ]8;;https://www.gnu.org/software/coreutils/tail#tail--help\[1m--help[0m]8;;\
         display this help and exit
      ]8;;https://www.gnu.org/software/coreutils/tail#tail--version\[1m--version[0m]8;;\
         output version information and exit

NUM may have a multiplier suffix:
b 512, kB 1000, K 1024, MB 1000*1000, M 1024*1024,
GB 1000*1000*1000, G 1024*1024*1024, and so on for T, P, E, Z, Y, R, Q.
Binary prefixes can be used, too: KiB=K, MiB=M, and so on.

With --follow (-f), tail defaults to following the file descriptor, which
means that even if a tail'ed file is renamed, tail will continue to track
its end.  This default behavior is not desirable when you really want to
track the actual name of the file, not the file descriptor (e.g., log
rotation).  Use --follow=name in that case.  That causes tail to track the
named file in a way that accommodates renaming, removal and creation.

Report bugs to: bug-coreutils@gnu.org
GNU coreutils home page: <https://www.gnu.org/software/coreutils/>
General help using GNU software: <https://www.gnu.org/gethelp/>
Full documentation <https://www.gnu.org/software/coreutils/tail>
or available locally via: info '(coreutils) tail invocation'
