Usage: rm [OPTION]... [FILE]...
Remove (unlink) the FILE(s).

  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm-f\[1m-f, --force[0m]8;;\
         ignore nonexistent files and arguments, never prompt
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm-i\[1m-i[0m]8;;\
         prompt before every removal
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm-I\[1m-I[0m]8;;\
         prompt once before removing more than three files,
         or when removing recursively; less intrusive than -i,
         while still giving protection against most mistakes
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm--interactive\[1m--interactive[=WHEN][0m]8;;\
         prompt according to WHEN: never, once (-I), or always (-i);
         without WHEN, prompt always
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm--one-file-system\[1m--one-file-system[0m]8;;\
         when removing a hierarchy recursively,
         skip any directory that is on a file system different
         from that of the corresponding command line argument
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm--no-preserve-root\[1m--no-preserve-root[0m]8;;\
         do not treat '/' specially
      ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm--preserve-root\[1m--preserve-root[=all][0m]8;;\
         do not remove '/' (default);
         with 'all', reject any command line argument
         on a separate device from its parent
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm-r\[1m-r, -R, --recursive[0m]8;;\
         remove directories and their contents recursively
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm-d\[1m-d, --dir[0m]8;;\
         remove empty directories
  ]8;;https://www.gnu.org/software/coreutils/manual/coreutils.html#rm-v\[1m-v, --verbose[0m]8;;\
         explain what is being done
      ]8;;https://www.gnu.org/software/coreutils/rm#rm--help\[1m--help[0m]8;;\
         display this help and exit
      ]8;;https://www.gnu.org/software/coreutils/rm#rm--version\[1m--version[0m]8;;\
         output version information and exit

By default, rm does not remove directories.  Use the --recursive (-r or -R)
option to remove each listed directory, too, along with all of its contents.

Any attempt to remove a file whose last file name component is '.' or '..'
is rejected with a diagnostic.

To remove a file whose name starts with a '-', for example '-foo',
use one of these commands:
  rm -- -foo

  rm ./-foo

If you use rm to remove a file, it might be possible to recover
some of its contents, given sufficient expertise and/or time.  For greater
assurance that the contents are unrecoverable, consider using shred(1).

Report bugs to: bug-coreutils@gnu.org
GNU coreutils home page: <https://www.gnu.org/software/coreutils/>
General help using GNU software: <https://www.gnu.org/gethelp/>
Full documentation <https://www.gnu.org/software/coreutils/rm>
or available locally via: info '(coreutils) rm invocation'
